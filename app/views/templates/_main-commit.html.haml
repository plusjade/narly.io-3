%script{ id: "commit-container-template", type: "x-template" }
  %h1.title {{ title }}
  .body-wrap
    .body {{{ body }}}
  .hunks-wrap
    {{# hunks }}
    %div{ class: "diff-file {{ status }}" }
      .diff-header
        %span {{ path }}
      .diff-content
        {{{ html }}}
    {{/ hunks }}
